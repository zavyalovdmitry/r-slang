import React from 'react';
import Logo from './Logo';
import Menu from './Menu';

const Header = () => (
  <header className="header w-1920">
    <Logo />
    <Menu />
  </header>
);

export default Header;
